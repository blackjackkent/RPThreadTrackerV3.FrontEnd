import React from 'react';

const NoThreadsMessage = () => (
	<div className="dashboard-card-center-message">
		You do not currently have any threads tracked.
		<br />
		Click &quot;Track New Thread&quot; in the header menu to add a thread to your account.
	</div>
);

export default NoThreadsMessage;
