import React from 'react';

const NoCharactersMessage = () => (
	<div className="dashboard-card-center-message">
		You have not yet added any characters to your account.
		<br />
		Click &quot;Add Character&quot; in the header menu to add one.
	</div>
);

export default NoCharactersMessage;
